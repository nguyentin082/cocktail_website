<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= cocktail.strDrink %></title>
    <link href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            padding-top: 20px;
            padding-bottom: 20px;
        }

        .dropdown-menu {
            max-height: 200px;
            overflow-y: auto;
        }

        .alphabet-links .btn {
            margin: 2px;
        }
    </style>
</head>

<body>
    <div class="container">
        <h1 class="mt-4"><%= cocktail.strDrink %></h1>
        <img src="<%= cocktail.strDrinkThumb %>" alt="<%= cocktail.strDrink %>" class="img-fluid mb-4" width="400">
        <h2>Instructions</h2>
        <p><%= cocktail.strInstructions %></p>
        <h2>Ingredients</h2>
        <ul>
            <% for(let i = 1; i <= 15; i++) { %>
            <% if (cocktail[`strIngredient${i}`]) { %>
            <li><%= cocktail[`strIngredient${i}`] %> - <%= cocktail[`strMeasure${i}`] %></li>
            <% } %>
            <% } %>
        </ul>
        <a href="/cocktails" class="btn btn-primary">Back to Cocktails</a>
    </div>
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.4/dist/umd/popper.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>

</html>